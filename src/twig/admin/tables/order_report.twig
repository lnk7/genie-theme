CREATE TABLE {{ tableName }} (
{% for field,settings in columns %}
    {{ field }} {{ settings.sql }},
{% endfor %}
{% for field,settings in columns %}
    {% if settings.key %}
        {{ settings.key }} {{ field }} ({{ field }}){{ loop.last ? '':',' }}
    {% endif %}
{% endfor %}
) {{ charset_collate }};