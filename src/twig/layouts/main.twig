<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" {{ theme.language_attributes() }}>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ theme.wp_title('|', true, 'right') }}</title>
    <script> window.site = '{{ site|json }}'</script>

    {{ theme.wp_head() }}
    {% block js %}
    {% endblock %}
</head>

<body>
<div id="root" class="loaded" v-cloak>
    {% block content %}
    {% endblock %}

</div>

{# wp footer #}
{{ theme.wp_footer() }}
</body>
</html>
